/*!
 * DataTables Bootstrap 5 integration
 * 2020 SpryMedia Ltd - datatables.net/license
 */

// Initialize the $jscomp object if it doesn't already exist
var $jscomp=$jscomp||{};

// Define a scope object for the $jscomp object
$jscomp.scope={};

// A function that finds the first index in an array that matches a condition
$jscomp.findInternal = function(a, b, c) {
  // implementation here
};

// Define various properties and methods on the $jscomp object
// ...

// Define a polyfill for the Array.prototype.find method
$jscomp.polyfill("Array.prototype.find", function(a) {
  // polyfill implementation here
}, "es6", "es3");

// Extend the default options for the DataTables library
a.extend(!0, d.defaults, {
  dom: "<'row'<'col-sm-12 col-md-6'l><'col-sm-12 col-md-6'f>><'row'<'col-sm-12'tr>><'row'<'col-sm-12 col-md-5'i><'col-sm-12 col-md-7'p>>",
  renderer: "bootstrap"
});

// Define additional classes for the DataTables wrapper and various elements
a.extend(d.ext.classes, {
  // class definitions here
});

// Define a custom renderer for the pagination buttons in DataTables
d.ext.renderer.pageButton.bootstrap = function(f, l, A, B, m, t) {
  // implementation here
};
